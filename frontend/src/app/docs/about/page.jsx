'use client';
import React from 'react';

const About = () => {

  return (
    <div>
      <div className='container py-10 px-40'>
        <section className='mb-10'>
          <h2 className='text-3xl font-bold'>What is React-Render</h2>
          <hr className='mt-2 mb-6 border-slate-400' />
          <p>A React-Render allows you to generate HTML dynamically using a template. React-Render uses predefined templates with placeholder variables that are then replaced with dynamic data. 
Existing templating engines often require developers to mix HTML with complex JavaScript syntax, leading to convoluted code. This complicates readability, maintainability, and hampers the separation of concerns, hindering efficient development and collaboration. React breaks away from the classic templating model by introducing a component-based structure and the power of JSX. In React, JSX serves as a templating engine where you can write HTML-like syntax directly without JavaScript.
</p>
            
          
        </section>
      </div>
      </div>
    
  )
}

export default About